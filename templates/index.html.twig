{% extends 'base.html.twig' %}

        {% block title %}
        Welcome!
        {% endblock %}

        {% block body %}

        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="aloitelaatikko">Home</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                <a class="nav-item nav-link active" href="aloitelaatikko/uusi">Uusi aloite <span class="sr-only">(current)</span></a>
                <a class="nav-item nav-link" href="#">Pricing</a>
    </div>
  </div>
</nav>
        
        <h1>Aloitelaatikko</h1>
        <table class="table table-hover">
  <thead>
        <tr>
      <th >ID</th>
      <th>Aihe</th>
      <th>Kuvaus</th>
      <th>Kirjauspäivämäärä</th>
        <th>Etunimi</th>
        <th>Sukunimi</th>
        <th>Email</th>
     <th>Toiminnot</th>
    </tr>
    </thead>
    <tbody>
    {% for aloite in aloitteet %}
    <tr>
      <th>{{ aloite.id }}</th>
      <td>{{ aloite.aihe }}</td>
      <td>{{ aloite.kuvaus }}</td>
      <td>{{ aloite.kirjauspvm }}</td>
      <td>{{aloite.etunimi }}</td>
      <td>{{aloite.sukunimi }}</td>
      <td>{{aloite.email }}</td>

      <td>
      <a href="#" class="btn btn-dark">Nayta</a>
      <a href="#" class="btn btn-success">Muokkaa</a>
      <button class="btn btn-danger">Poista</button>


     </td>
    </tr>
    {% endfor %}
  </tbody>
</table>
        {% endblock %}
